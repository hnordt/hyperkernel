/**
 * Public kernel API.
 *
 * This package is intentionally empty while the initial workspace boundary is
 * verified. Domain contracts will move here in later, separately reviewed steps.
 */
export {};
